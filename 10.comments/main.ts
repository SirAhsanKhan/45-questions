//adding comments 
let comments = "comments are the non runable syntax which is only used by the coder"//explaining comments
let message = (`comments can be defined as ${comments}`)//this store message containing explaination of comments
console.log(message);//this prints the message