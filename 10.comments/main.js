//adding comments 
var comments = "comments are the non runable syntax which is only used by the coder"; //explaining comments
var message = ("comments can be defined as ".concat(comments)); //this store message containing explaination of comments
console.log(message); //this prints the message
