let anime: { name: string; genre: string; episodes: number; rating: number } = {
  name: "jujutsu kaisen",
  genre: "action/shonen",
  episodes: 23,
  rating: 9.5 / 10,
};
console.log(
  `animeInfo: title ${anime.name} type of anime ${anime.genre} with ${anime.episodes} episodes i rate this ${anime.rating}`
);
