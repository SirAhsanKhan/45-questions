var transport = ["rolls royce", "BMW", "lambo"]; //this save an array
transport.forEach(function (transport) {
    console.log("i would like to own a ".concat(transport)); //this  will print message for each transport
});
